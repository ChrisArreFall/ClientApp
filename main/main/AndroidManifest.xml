<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tec.restarurante.clienteapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Inicio"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".platillo"
            android:label="@string/title_activity_platillo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OrdenActual"
            android:label="@string/title_activity_orden_actual"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CheckOut"
            android:label="@string/title_activity_check_out"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Comment"
            android:label="@string/title_activity_comment"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>